# Rodar os testes

```bash
pytest
```

# Rodar com coverage no terminal

```bash
pytest --cov=. --cov-report=term-missing
```

# Rodar com coverage no browser

```bash
pytest --cov=. --cov-report=html
```
